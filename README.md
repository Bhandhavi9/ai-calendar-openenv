AI Calendar Scheduling Environment (OpenEnv)

This project implements a real-world OpenEnv environment where an AI agent learns to schedule meetings across multiple participants with constraints such as timezones, availability, and priority (VIP users).

Unlike toy environments, this system simulates a practical scheduling problem similar to real-world tools like calendar assistants and enterprise meeting planners.
